<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required for foreground services that play media on Android 34+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Permissions needed to query Wi-Fi and network state for presence detection -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Required to read connected Wi‑Fi SSID (runtime permission required on modern Android) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:name=".SecurityApp"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecurityApp"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SecurityApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".SecurityActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".ScadaActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".IPERLActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".MainActivity" />
            
        <!-- Google Sheets Graphing Activities -->
        <activity
            android:name=".WeatherGraphsActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".ScadaActivity" />
            
        <activity
            android:name=".GeyserGraphsActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".ScadaActivity" />
            
        <activity
            android:name=".PowerGraphsActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".ScadaActivity" />
            
        <activity
            android:name=".DvrGraphsActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".ScadaActivity" />

        <activity
            android:name=".MikeWaterGraphsActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".IPERLActivity" />

        <activity
            android:name=".AlarmTestActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".IDSActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".MainActivity" />

        <!-- Full-screen alarm: ensure the exact class used in code is declared so a pending intent or direct startActivity works -->
        <activity
            android:name=".AlarmFullscreenActivity"
            android:exported="true"
            android:theme="@style/Theme.SecurityApp" />

        <activity
            android:name=".ImageViewActivity"
            android:exported="false"
            android:theme="@style/Theme.SecurityApp"
            android:parentActivityName=".SecurityActivity" />

        <service
            android:name=".NotificationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".NotificationActionReceiver"
            android:exported="false" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- Suppress Firebase component discovery build notifications -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />

    </application>

</manifest>